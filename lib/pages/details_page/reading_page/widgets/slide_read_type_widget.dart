import 'package:flutter/material.dart';

class SlideReadTypeWidget extends StatelessWidget {
  const SlideReadTypeWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
